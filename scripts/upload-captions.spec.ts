import {uploadCaptions} from "./upload-captions";


describe('GIVEN: there is a file in the designated folder on desktop', () => {
  describe('WHEN: the function is executed,', () => {
    test('THEN: the file is uploaded to its bucket in S3.', async () => {

    });
  });
});
